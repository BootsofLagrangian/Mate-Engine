# Real LM appearance Windows probe

Root exclusively launches `native/tools/probe_windows_appearance_skills.gd`. Required arguments are `--output ABS_DIR --default-sha256 SHA256 --wet-sha256 SHA256`, optionally `--character mambo|hachimi` (Mambo default). Supply hashes of the exact packaged/served current VRMs; hashes must differ. No native test launch has occurred here.

The probe reuses the strict space-skill probe's actual PCM playback tracking, raw backend events, owned viewport capture and settings restoration. Explicit direct default setup establishes the measured baseline. Both measured changes then use real Korean chat: “젖은 외형으로 바꿔 줘. 먼저 짧게 대답해 줘.” and “원래의 기본 외형으로 돌아가 줘. 먼저 짧게 대답해 줘.” No model response or intent is injected. The second request runs after the first actually reaches the expected wet bytes; failed attempts and raw events remain in the report.

Per turn it requires successful Japanese reply text, positive native PCM playback/envelope, matching streamed action/done `change_appearance` intent and expected variant ID, exactly one native completed outcome, accepted raw backend feedback acknowledgment, actual loaded VRM hash different from the previous one and equal to the supplied expected hash, matching variant cache path, unchanged selected character/session instance and no connection loss. Japanese text plus PCM is playback evidence, not independent audio transcription. Original settings are restored under a 120-second overall watchdog.

Output retains `report.json` with both prompts/turn IDs, stripped raw events (PCM byte counts instead of base64), outcome/ack records, exact byte hashes and own viewport screenshots. Root must inspect the actual Windows images and response evidence. Passing simulated transaction tests does not establish this live skill. Source parse passed. Independent reviewer `character_package/package_review` APPROVED the final probe after correcting an absolute-path versus `user://` cache comparison. Both compared paths now globalize and simplify before equality. Windows results remain pending.
