# Workstation chair choreography (integration candidate)

The computer asset declares a movable swivel chair. Setup is automatic digital furniture movement; the character does not claim an authored hand-pull action. The installed authored entry/exit clips retain their source joint curves and backwards root progression.

The host prepares the empty chair by a bounded pull and swivel, approaches the resulting source-derived standing target through ordinary scene navigation, and enters using the authored clip. While seated it lifts the feet, waits for a fresh rendered sole-clearance result, swivels and rolls the chair to the keyboard, stops the support, and lowers the feet before the finite ten-second work interaction. Normal completion reverses the carrier sequence, plays authored exit, holds the exact committed world foot, and restores the empty chair before emitting completion.

Quintic interpolation has zero endpoint velocity and acceleration. Lift/lower each take at least 0.6 seconds. Translation is bounded by 0.16 world metres/second and swivel by 70 degrees/second; long frames advance at most 0.05 simulation seconds. The common 125-degree swivel lasts about 3.35 seconds. A full normal use-to-completion path takes about 21 seconds, including the ten-second work interval. An accepted workstation setup has an 80-second interaction deadline; the route itself retains its existing 30-second limit. These durations describe intended finite phases, not a reason to accept missing terminal events.

New workstations retain the desk’s original physical proportions. A separate persisted `seat_scale` fits only the adjustable chair to the installed source’s seat clearance; existing global object sizes are preserved, and ordinary chairs/sofas keep their prior sizing rule.

The setup planner retains actual desk/chair components. Semantic placement may seek a bounded fixed-Y X/Z location fitting the whole setup envelope, then rerun the complete plan. Explicit position requests are not silently relocated. Native/shared rendering transfers the actual chair setup on interruption; the chair does not reset merely because shared rendering ends. Derived chair setup is a runtime interaction state, separate from persisted object position/scale/yaw.

Validation scope: the deterministic host fixture checks stale-readiness rejection, next-render readiness, phase order, exact setup endpoints, and reverse motion before authored exit. The scene fixture checks restoration foot ownership and callback guards with a real rig. Motion's independent carrier tests measure sole clearance and additive pose continuity. The host requires a stationary articulated lift/lower envelope before changing foot lift and a frozen body-volume sweep before rigid swivel/roll. Both gates include fixed desk components and other objects, and their projected envelopes must fit the usable screen. The source/model hold identity is checked during travel. Actual combined geometry tests and Windows end-to-end completion remain required before acceptance. Hair and garment collision are not established by a bone-derived body-volume test.

## Clearance after standing

The first strict Windows candidate reached work and authored exit, then correctly rejected empty-chair restoration because the standing actor occupied the swivel sweep. It did not complete successfully. The corrected host may insert one short authored scene walk before empty restoration. A same-Y destination within `0.5 * pet_scale` metres must clear both chair restoration sweeps and a route through all current solids; both its complete path and destination must fit the standing view. The helper’s exact navigation area and resolution are revalidated by the live adapter, rather than trusting a stale path or substituting a differently aligned grid.

The chair stays still during departure. A token-bound owned callback accepts only actual arrival, records the new walked endpoint, and reacquires its exact ground latch before restoration and terminal feedback. Failure, eight-second cleanup timeout, or user interruption cannot become `completed`; an old callback cannot resume a newer interaction. The original authored exit point remains recorded separately. In the recorded Cheval geometry, the required step is 0.10 metres.

`test_restore_walk.gd` passes 12 checks with the real Cheval rig, MotionPlayer, navigation adapter and furniture geometry, including restoration after actual walking and cancellation/stale callback behavior. Its viewport-fit/UI seams are fixtures; Windows presentation remains separately tested. The fixture retains an ObjectDB warning at exit.

## Recorded Windows acceptance: strict full cycle

Independently inspected `logs/windows-full-computer-restore-cache/{report.json,samples.json,launch.json}` and final own-viewport frame `frames/003347.png`. This Cheval run used executable SHA-256 `663d320c485e22a81efe9e6fddcedc1442daacf30a78cecae27e3eb7fd96d2a6`, verified model SHA-256 `61e40e14eec93f9bd8124b2a9d3a71aba0e580ee9b2ef4a55dbb6e4ce5d8ef8a`, and recorded **74 checks, zero failures**. There is exactly one computer terminal: `completed` at 74,760 ms.

The 51 `restore_approaching` frames span 69,220–70,043 ms and retain token `70345555` and scene foot ownership throughout. Actual foot travel covers 0.10099 m of sampled path, with exactly constant recorded world Y −1.330282092 m. At 70,068 ms the arrived foot, `completed_foot`, and `contact_exit_world` all equal `(1.859327435, −1.330282092, −0.015794434)`; restoration starts only after this endpoint handoff. The empty-chair step remains absent during walking. The subsequent recorded steps restore swivel from approximately 145° to 0°, then pullout from 0.1 to 0; idle follows at 74,776 ms. The final image shows the standing character clear of the returned chair and the chair beside the desk. Step targets and the completed lifecycle support the endpoint state; this probe does not serialize an independent final native-window chair-transform measurement.

The post-completion hold lasts 5,016 ms with retained ownership and **zero recorded XYZ drift** from the walked endpoint. This establishes the strict scenario's real cleanup walk, terminal ordering and ground hold. It does not establish success from every initial placement: the separate product-default LM run subsequently reported `setup_view_blocked` and remains separate evidence. Bone-derived collision checks do not establish complete garment collision.

The subsequent product-default failure exposed a projection-contract mismatch: setup admission projected transformed local-box corners, while runtime fitting projected the enclosing world-axis-aligned box. At the reconstructed recorded placement, the former right edge was approximately 2522.7 px and the runtime rest bounds reached 2616.4 px on a 2560 px workarea. `_chair_setup_fits` now projects the runtime world box merged with the transformed swept envelope, preserving all existing area limits. `test_setup_projection_bounds.gd` passes six checks using the actual workstation asset, including the old false admission and a feasible inward placement. This source correction requires separate product-default Windows acceptance; the strict run above predates it.

The next product-default run (`logs/text-scenario-computer-product-envelope`) passed chair setup but rejected an approach step at 26,393 ms: the turning actor's occupied rectangle reached X=2560.329 px on the 2560 px workarea. Furniture fit alone had not admitted the actor route. The new preflight checks the cached all-heading avatar envelope at every planned path vertex and requires each adjacent pair's merged projected rectangle to fit one workarea. At positive camera depth this encloses every projected hull vertex along the straight world segment, including intermediate heading changes. Semantic placement includes the target actor envelope and tests the complete fresh plan before committing a candidate. The live adapter rebuilds the same accepted navigation grid and checks its route again before replacing ownership.

The existing one-time legacy-to-scene ground placement now occurs before empty-chair setup. It records original/normalized foot and projected pixel displacement, preserves world Y, and validates the correction segment against all current solids. Already-owned ground remains exact. This is explicit admission placement, not authored travel or a continuous correction; any visible displacement must not be counted as natural walking. Exact live authored-entry bounds remain a later admission gate, not a claimed precomputed guarantee.

Focused source checks: `test_approach_route_view.gd` 5/0 (actual Cheval/recorded rejected point, feasible inward route, no actor mutation, monitor-gap rejection); scene host 287/0 (including explicit admission diagnostics and exact repeated ground ownership); actual restoration walk 12/0. The latter two fixtures retain their exit ObjectDB warning. Product-default Windows success remains pending for this revision.

The reconstructed product placement fixture rejects the original route, then selects a further 0.146316 m fixed-Y workstation relocation. Its fresh complete route, 62 setup samples and fixture-specific live entry envelope fit. The six complete candidate plans took 2.45 seconds on Linux, a disclosed one-time synchronous planning cost. This is not a rendered motion result or a guarantee about the future live entry pose. The fixture's initial existing admission policy corrected the unowned ground anchor by 0.093198 m inward Z; already-owned anchors are not adjusted by setup.
