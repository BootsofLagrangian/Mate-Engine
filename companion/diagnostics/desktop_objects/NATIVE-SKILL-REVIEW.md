# Native furniture skill and occupied-view source review

Independent review against baseline 3107e1ab; source only, distinct from the earlier 265-check furniture package. No runtime edits by reviewer.

**REVISE: user navigation priority across controllers.** ObjectsHost.request_intent rejects an LLM call when ObjectsHost owns pending/active user work, but does not inspect the Living director's active/queued user navigation. A queued LLM furniture use can dispatch while an explicit user destination is active, then interact calls living.cancel("object_interaction") and cancels that user destination. Reject or defer across both controllers; preserve user ownership at dispatch as well as receipt and add a regression for user move followed by LLM furniture. Finding sent to owner.

Reviewed bounded registry and typed target publication, finite parameter/preset validation, queued command expiry/character cleanup, live target-type revalidation before dispatch, action/done same-kind dedup, terminal feedback routing, per-instance appearance restoration and native projection changes. No other material source blocker identified in that scope. Unknown source/kind/extra native intent fields are currently tolerated by the direct host API, unlike the stricter backend: tightening this boundary is recommended.

Occupied view refresh computes shared scene floor from the real avatar seat and transformed local socket, then uses camera-aware depth inversion; whole arrangement admission and owned-seat reprojection are explicit. refresh_seat_projection cannot acquire a new seat: current ownership/support identity and subpixel projected coincidence must match, and visible body bounds must remain safe. The projected physical floor may change under camera orbit; this is disclosed and is not a promise of a fixed screen-floor pixel across arbitrary view angles. Per-instance materials duplicate source resources and reset from retained originals. New standalone pixels-per-metre invariants and rotation-expanded viewport sizing require the separately planned actual Windows checks; source tests do not establish compositor alpha or final visual ergonomics.

## Corrected arbitration, view bounds and feedback acknowledgement

**APPROVED bounded corrected source scope.** Re-read Director.has_user_intent and both ObjectsHost intake/dispatch guards. Active and queued unexpired user navigation now prevent LLM furniture mutation; a user request arriving after furniture enqueue is checked again before dispatch. The focused regression covers started, queued and later-arriving user ownership and verifies no object creation. Source/kind/extra-field validation also closes the direct API robustness observation. The historical REVISE above is resolved.

The occupied-view correction preserves physical scale and local pose while translating the entire assembly by the same integer displacement in root/autonomy origins and shared floor. Its bounded solver chooses the smallest Euclidean translation into an intersecting workarea, rejects excessive (>256 px) movement and oversize/disconnected cases, and rechecks containment. It does not count physical assembly movement as transparent reframing. The actual-geometry reproduction demonstrates the specific 52.59 px overflow corrected by 53 px upward movement with seat coincidence and idempotence; this is source/geometry evidence, not final Windows replay approval.

Session now accepts intent_result as a control acknowledgement alongside world_context. It does not assign a foreground turn or change character/activity/history itself; normal event listeners receive the acknowledgement. No material blocker identified in these three corrected deltas. Final packaged visual, camera, skill execution and compositor acceptance remain separate.
