# Authored entry admission review

APPROVED for source and bounded admission tests. Ordinary immediate seat admission and final attachment retain the 48 px rule. The separate authored-entry path checks the captured skeleton/clip instances, source duration/translation, physical scale, furniture facing, seat identity/position and current standing support. Its planned staging must match within the existing 24 px legacy approach tolerance; perspective additionally requires scene ownership and XZ distance within 15 mm of the planned target. Existing policy, surface alignment, seat span, destination coverage and full projected path checks remain.

Independent generic test run: **46 passed, zero failed**. Reviewed the real Cheval/UMA01/workstation fixture and its **10 checks, zero failures** log: actual source at working yaw produces 60.4003 px travel, rejected by immediate admission but accepted at zero authored-staging residual. Unplanned drift, lost support, moved seat, scale changes and replacement rig/clip instances reject the snapshot.

The real-asset fixture explicitly positions the avatar and supplies a large workarea to isolate admission; it does not reproduce the production desktop route or prove visual completion. It retains an ObjectDB exit warning. Camera geometry is re-evaluated at admission rather than camera-fingerprinted. Actual Windows Korean-request completion and authored transition rendering remain separate evidence.
# Observational preparation timing follow-up

Astra Face independently reviewed `_begin_seat` and `_start_presentation` timing additions on 2026-09-07: **APPROVED**. Geometry preparation, admission, scene release, Motion start and contact-view fit are each evaluated once in their original order. New monotonic timing fields, reason labels and copied preparation dictionaries do not change admission thresholds or return behavior. Inspected `/tmp/authored-preparation-timing.log`: 172 checks, zero failures, with the retained fixture exit warning. This does not certify actual Windows preparation latency. On unsuccessful presentation start, detailed timing remains in `presentation_preparation`; it is copied into `entry_preparation.presentation` only after success.
