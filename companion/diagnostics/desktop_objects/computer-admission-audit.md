# Computer admission audit

2026-09-07, Astra prop-design owner. This audit adds rejection diagnostics; it does not change thresholds, policy or asset geometry, and does not claim computer use is fixed.

## Retained orthographic failure

`logs/text-scenario-computer-ownerfix-ko-01` ran executable SHA-256 `d9029b0e00fb85e12998243dd90128fb57eb26765ac48c7e2051ea40e441ccdd`. The real Korean request produced the correct computer-use intent and Japanese reply `用意してみますね。`, with 81,280 PCM bytes. Internal approach completed at 20,235ms. Furniture ended `unsafe_seat` at 23,657ms.

Facing contexts from 20,392–23,503ms show `blocked=true` while recorded session foreground busy, speaking, panel and pointer interaction are false. This alone does **not** identify the failed gate: these samples are 250ms apart and do not capture the brief `ready_contact` phase. The failed build's retained manifest at `logs/windows-default-scene-idle-admission/build-manifest.json` proves its ObjectsHost (`f8f8eff4…`) and DesktopAutonomy (`4a596d3a…`) matched the pre-diagnostic source inspected here, including the explicit facing→ready_contact context refresh. Its main hash differs from the later current build. Therefore claiming the facing hold itself is the cause would overstate the evidence.

No authored entering phase was observed. The initial admission check runs before `_start_presentation`; source-envelope preparation failures have other outcomes. Remaining actual guard possibilities include blocked context, pointer/braking policy, stale/missing seat line, point/line-Y mismatch, >48px horizontal distance, seat span, destination workarea coverage or intermediate path coverage. The old report does not record enough geometry to choose one.

## Separate perspective placement failure

`logs/windows-scene-navigation-owner/report.json` reports computer `last_creation.reason=created`, computer `obj_2` sizing metadata, then `no_space` at 28,853ms after a command at 28,557ms, with interaction idle. This identifies the post-creation `_place_command_object` failure branch, before interaction/seat admission. The retained `view` and `scene_staging` diagnostics are earlier chair values and cannot establish computer fit. The new diagnostic records the candidate projected window and workareas before rollback overwrites its live geometry.

## Diagnostic contract

`DesktopAutonomy.last_seat_admission` records the exact existing boolean guard branch, with flags, seat surface, point, anchor, origin, horizontal distance, allowed span, effective visible/start/destination bounds, workareas and computed destination/path decisions. Nonfinite inputs are represented as strings in diagnostic geometry, preserving valid JSON. `request_seat_contact` records a fresh `wrong_contact_pose` reason instead of leaving stale admission data.

Before clearing `unsafe_seat`, ObjectsHost retains this record under `fit_diagnostics.seat_admission` and prints one structured `FURNITURE_ADMISSION` JSON line. `no_space` prints creation, semantic placement and spatial rejection records. The spatial candidate record distinguishes missing camera/window, invalid scale/legacy position, projection failure and a projected native window outside every single usable monitor. Transaction rollback preserves both failed candidate and restored projection records. Legacy placement records its requested pixel rectangle and whether the existing move operation rejected it.

Validation: `test_seat_admission_diagnostics.gd` **38/0**, comparing the original guard's return value across policy/geometry cases and checking named rejection/valid JSON; furniture command regression **31/0**; full native selftest **627/0**. Initial diagnostic fixture mistakenly retained the module's default blocked state; that test setup was corrected explicitly, without changing production policy. Final additional early-return labels passed source parsing. Independent reviewer `astra_face` APPROVED the final diagnostic delta and independently reran 38/0. The review notes that aggregated `last_*` helper records can be historical when their path was not executed for a command; interpret the current object ID and placement reason, and do not treat an older helper record as fresh evidence. Root alone launches Windows; next actual rejected record is needed before proposing a behavior fix.

## Exact packaged rejection records

Root's saved-settings Korean replay `logs/text-scenario-computer-admission-diag-ko` now identifies the branch: `seat_too_far_horizontally`, **60.400146px** against the ordinary immediate-contact limit of 48px. The current `ready_contact` record has `blocked=false`, pointer interaction and braking false, enabled surface mode, a registered `object:obj_2:seat`, and zero surface-Y error. Its endpoint rectangle `(2021.7,945.9223)+(532.238,441.8214)` fits the 2560×1392 workarea. This is not the earlier speculative facing-policy rejection. The effective endpoint fits numerically; this failed execution did not reach the later path predicate.

The legacy approach already derives its standing-foot target from final working yaw and authored hips displacement. Cheval's installed UMA01 entry delta is `(-.003032621,-.342931211,-.312393785)` target-local metres. At working yaw 215°, its rotated X component is approximately `.18166589m`. This deliberately stages the actor in front of the cushion before backwards source travel. Replacing that target with the final seat position would erase the authored movement. The incompatible part is the ordinary immediate-contact distance check before the authored clip starts.

The separate product-default perspective replay `logs/text-scenario-computer-product-diag-ko` successfully creates `obj_1`, then rejects semantic `near` placement. Requested position `(4.045896,-1.918900,-0.00000024)` projects its native object window to `(2391,1300)+(193,327)`; rollback position `(3.625896,-1.918900,-0.00000024)` projects to `(2282,1300)+(197,327)`. Both overflow the primary workarea floor by 235px; the requested candidate additionally crosses its right edge. All retained contexts from 6611–9008ms have `support.attached=false`, initially settling then paused by the reply. Placement currently copies this live foot Y and applies one +.42m near-X shift. Authored workstation geometry has floor Y=0 (legs 0–.706m), so no hidden negative-Y mesh explains this overflow. Ground ownership/readiness and fixed-ground XZ feasibility are a separate change owned by Autonomy. A later post-chair fixture also fails near the floor, so merely waiting for `attached` or trying the opposite X side is not sufficient evidence of a correct physical-ground placement.

## Authored-entry admission change

`can_request_authored_seat_entry` now validates the planned standing-seat projection within the existing 24px approach-arrival tolerance, while retaining policy, registered seat/Y, support span, destination and intermediate workarea coverage. `can_request_seat_contact` and terminal attachment retain the original 48px limit. The host captures actual installed skeleton and clip instance IDs, clip name/duration/source delta, character scale, facing, seat and projection mode before approach. A stale rig/source/scale/facing/seat plan is rejected. Entry additionally requires attached standing-foot support or a scene-owned ground anchor; scene entry rechecks the actual XZ arrival against its target within the existing .015m tolerance. Current camera projection and bounds are recalculated at admission rather than claiming a stored camera fingerprint.

The host still starts the actual registered source timeline and validates its full presentation envelope before entering. This change grants no immediate pose-only seat fallback, no larger generic attachment radius, and no fake source target. Root's next packaged temporal execution is required to establish live completion and natural transition quality.

Focused validation: expanded boolean/policy/geometry fixture **46/0**; actual installed Cheval + UMA01 + premium workstation fixture **10/0**, retaining source vector and projection in `authored-admission-final.log`. The real-asset fixture reproduces **60.400269px**, confirms ordinary rejection and authored admission with **0px staging residual**, and rejects a 25px unplanned drift, missing ground, seat movement, changed scale, a same-name clip replacement and a same-path rig reload. Its first fixture setup had two failures because the computer's 35° presentation rotation was not applied and seat registration cleared the stub floor; the setup was corrected to the production geometry/attached-foot precondition. The passing fixture tests admission only; it does not claim an actual Windows seating sequence or solve the separate placement/heel-height issues.
