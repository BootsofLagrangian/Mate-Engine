# Workstation carrier geometry, 2026-09-07

The frozen runtime retains the professional desk at its original relative size and fits only the declared adjustable chair using persisted `seat_scale`. No desk solid, capsule radius, keyboard target or source joint curve was removed to obtain clearance. This is automatic chair preparation, not an authored hand-pulling action.

The final production-planner fixture uses the installed entry/idle clips, actual Cheval/Rice/Eishin rigs at avatar scale 0.6, desk scale 0.6, and chair ratios derived from each source seat clearance. Starting at world (-1,0,1), it selects entry yaw 0 degrees, chair swivel 145 degrees, and pullout 0.10/0.05/0.10 asset metres respectively. Source distance factors are 1.15/1.10/1.20. These are conservative component-AABB navigation choices, not a claim of globally minimal physical movement.

`occupied-planner-final.log` records all six rigid swivel/roll checks, three stationary lift/lower envelopes, and six actual hand contacts passing. Maximum wrist residual is 7.45e-8 world metres. The fixture actually plays the source before obtaining the frozen seated body snapshot. It validates geometric endpoints and swept body volumes; it does not execute the entire production host lifecycle or prove Windows temporal presentation.

`workstation-setup-final.log` contains 50 passing checks, including actual three-rig setup planning, unchanged keyboard geometry, actual cushion socket/facing, displaced-state preservation, reverse actor blocking, continuous envelope coverage, fixed-ground reposition proposals and view rejection. `computer-window-seat-scale-final.log` contains 87 passing native assembly/socket/crop checks across three global scales and orthographic camera bases/zoom. `carrier-sweep-geometry.log` contains 17 focused synthetic checks of intermediate translation/rotation collision, transformed component geometry, lifecycle identity, stationary envelope scope and mutation restoration.

Prior failures are retained. `occupied-carrier-sweep.log` and `occupied-carrier-clearance.log` used the rejected whole-workstation height adjustment. Increasing pullout cleared swivel but did not clear final apron contact. `occupied-thigh-mesh.log` independently found 12/20/22 actual skinned triangles crossing the front apron for Cheval/Rice/Eishin, with 9/2/13 vertices inside. `occupied-independent-chair.log` records zero triangle intersections and zero inside vertices after retaining desk height and adjusting only the chair. The archived diagnostic scripts preserve that explicit before/after experiment; they are not production setters. `occupied-chair-height-fixed.log` records a transient parse failure while the separate lift-envelope dependency was being authored, not a collision result. `occupied-minimum-setup.log` was the earlier fixed 90-degree entry check; the final planner fixture supersedes its manually chosen orientation.

The body-volume contract excludes hands, hair, garments and accessories. The held transport body is frozen; the separate analytical envelope covers stationary foot lift/lower. Keyboard reach is independently measured after lowering, but neither test proves collision-free later typing animation or full cloth contact. Actual Windows approach, chair setup, authored entry, occupied carry, work and exit must still be reviewed on the packaged runtime. Normal-chair cushion/boot/caster visual alignment is a separate audit.

## Empty-chair restoration after authored exit

The first packaged full-cycle run (`logs/windows-full-computer-carrier`) reached work and authored exit, then correctly rejected an empty-chair swivel across the newly standing actor. A separate helper now finds a short actual walking destination at unchanged world Y before attempting either restore phase. It checks the complete current-solid navigation route, candidate/route view callbacks, and both empty swivel and roll sweeps. It restores the original chair state on success or rejection and returns its exact navigation grid area/resolution for the host to revalidate against current geometry.

The reconstructed recorded 70147-ms actor/workstation sample reproduces `chair_sweep_hits_actor` and finds a 0.10-m move to world (1.819629, -1.330282, -0.004950), with both restoration phases clear. `recorded-restore-clearance-final.log` scopes this reconstruction to the recorded actor and workstation, excluding prior other furniture/camera. The production host must supply those current constraints. The extended actual three-rig helper fixture passes 68 checks (`workstation-restore-clearance-fine.log`). Each finds a 0.10-m move; Linux helper time was approximately 20–22 ms. The earlier 0.03-m local navigation grid rejected Rice at a cell boundary despite the point being outside real solids; `workstation-restore-clearance.log` retains that failure. The bounded helper uses the existing supported 0.02-m cell size without changing obstacle expansion, endpoint tolerance or physical solids. This is grid resolution, not permission to cross a collider. Final Windows restore/route integration remains pending.

## Product-default perspective crop correction

The subsequent real LM run `logs/text-scenario-computer-product-restore-cache` delivered the correct voiced action but failed at 19552 ms. Its setup plan had already been accepted at 19455 ms; the first animated runtime fit rejected the crop. The preparation callback projected transformed local-envelope corners, while `contact_bounds()` projected a world-axis AABB. Perspective cross-corners made these different representations disagree near the right monitor edge.

`probe_product_setup_view.gd` reconstructs the recorded target, camera, workarea and current installed rig calibration. The derived original assembly position is (3.987050, -1.314583, -0.709737). The old local setup projection ends at x=2522.70 pixels and passes the 2560-pixel screen; actual runtime rest world-AABB projection ends at x=2616.40 and fails. The full world-axis setup envelope extends to x=2679.62. The original transient object position was not independently serialized, so the reconstruction's position is explicitly derived rather than asserted as a captured value.

The paired fix uses the existing runtime world-AABB representation for setup admission and grounded reposition projection. Actual solid components remain unchanged. The corrected precheck rejects the original position, and the existing bounded fixed-Y solver proposes (3.819798, -1.314583, -0.941452), 0.285770 m away. Fresh setup planning accepts, and all 62 sampled actual pull/swivel runtime bounds fit. The continuous envelope remains conservative; sampling is additional regression evidence. Linux solver time was 1.404 ms; initial/fresh full setup planning took 437/292 ms in this run. These are not Windows frame costs. Old failure and final reproduction logs are retained here. Actual product-default LM completion after this correction remains pending.

## Complete approach admission after preparation

The next product-default LM run (`logs/text-scenario-computer-product-envelope`) completed the full empty-chair setup and then failed during scene walking at 26393 ms. The new terminal snapshot records the precise rejected actor rectangle: its right edge was 2560.329 pixels, beyond the 2560-pixel monitor. Thus the previous furniture-envelope correction worked; the missing guard was the actor approach footprint/path.

The planner now receives the actual all-heading avatar hull and checks the obstacle-derived navigation route before accepting a setup. Every route vertex must need no ground correction; adjacent projected endpoint hull rectangles must be contained in the same workarea. Positive-depth perspective projection of straight-segment hull trajectories remains inside that endpoint envelope. The live scene adapter revalidates the rebuilt route against current geometry using the accepted grid descriptor. Existing bounded ground normalization and its swept-solid check occur before empty-chair animation; there is no continuous route correction or collider exemption.

Grounded reposition includes the target actor hull alongside the furniture world-AABB envelope. Each geometric proposal must pass a fresh complete setup and route plan before selection; failed proposals do not mutate the live scene or store. The source-derived entry factor/yaw can change after relocation, so a previously admitted route is not blindly translated and reused.

`probe_product_approach_view.gd` uses the exact recorded object pose and camera, plus the installed Cheval rig. It separately discloses the initial foot reconstructed from the earlier recorded screen anchor. Existing normalization moves that initial foot 0.093198 m in Z at fixed Y. The old furniture-only plan reproduces the recorded target and rejects route vertex 8. The corrected search chooses (3.574639, -1.314583, -0.764495), 0.146316 m from the recorded object pose. Fresh planning admits its full continuous all-heading approach and 62 sampled actual setup poses. A real installed-source entry envelope measured from this fixture's current outgoing pose also fits, but it cannot certify the future Windows outgoing walk/secondary pose; the production live entry gate remains authoritative.

The final local run validates six candidate plans in 2.454 seconds, then its separately repeated fresh plan takes 263 ms. This synchronous setup cost is material and is not claimed to be frame-safe. Nearest geometric proposals are deduplicated and validated in distance order, but no speculative translated-path rejection replaces a fresh proof. The existing setup regression passes 68 checks and ground-placement regression passes 29; the latter retains its fixture ObjectDB-exit warning. Actual product-default LM completion after this route correction is still pending.

## Accepted product run and bounded optimization audit

Root's packaged run `logs/text-scenario-computer-product-route/report.json` passed 7/0, including real voiced LM action, full native completion at 62511 ms and accepted execution feedback. Its actual candidate search needed two validated proposals; the final fresh setup plan recorded 265.64 ms. This is functional acceptance of that run, distinct from a zero-stall performance claim.

Before adding a proposed target-hull prefilter, the six-candidate local reproduction was instrumented without changing runtime. Every candidate's selected target already passed the all-heading ground query with `changed=false`; the rejected plans failed along obstacle-route detours. The complete traced callback set contained twelve safe-endpoint/unsafe-route checks and three safe-endpoint/safe-route checks (including repeated fixture validation), and no unsafe endpoint. A necessary target-only rejection would not remove these measured navigation builds. The proposed filter was therefore not landed. `approach-candidate-trace.log`, `approach-route-trace.log` and their diagnostic scripts retain the evidence. The approved runtime remains unchanged; initial synchronous planning cost remains a limitation, separate from phase playback.
