# Body capsules and finite seated phase hold review

Verdict: **APPROVED for the declared rig-volume and finite body-hold contract**. Reviewed identities are in `review-body-capsules-identity.json`. This is not mesh enclosure or proof of the separate continuous sweep algorithm, desk placement, Windows timing, or visual quality.

RigBodyCapsules returns 15 avatar-local capsules for pelvis, lower/upper torso, neck, head, thighs, calves, feet, upper arms and forearms. Endpoints follow current FK; anatomical radii derive from rest dimensions. The contract explicitly excludes hands, garments, hair and accessories and labels these volumes approximations. Both endpoint transform and radius expansion account for skeleton-to-avatar transform. The corrected radius multiplier uses the largest absolute Gram-matrix row sum, square-rooted: a conservative upper bound for the largest singular value, including small-scale shear. An earlier tolerance-based orthogonality shortcut could underbound tiny sheared transforms; review identified and corrected that case.

The carrier captures composed humanoid rotations and hips, leaving eye/jaw animation outside the hold. It restores the captured body before floor correction and additive foot clearance and freezes the seated source phase during the finite hold. A ready-only snapshot certifies this held articulation; lifting/lowering themselves are outside the rigid ready interval. Fresh snapshots do not share capsule dictionaries with previous callers.

Independent `review_body_capsules.gd` passes all five real rigs through authored entry, a nonzero 1.0-second seated phase, eased lift, 0.3 m translation plus 90-degree yaw, lowering and resume. It retains whole-shoe clearance gates and checks every snapshot is read-only, has 15 finite positive-radius capsules, remains independent of caller mutation and stays stable in avatar-local coordinates. Maximum endpoint drift is 6.664e-8 m; skeleton matrix/origin mutation is exactly zero. Held phase 1.0 s resumes at 1.0166667 s on the next 60 Hz step. Maximum lowered foot handoff is 0.0421 mm. These results concern the selected paths/heights and do not change the prior Mambo strict authored-entry limitation.

Additional independent checks cover 360 planar directions at each of three shear scales (0.001, 1 and 3), absence of a ready body proof during early lift, null-avatar snapshots, rejected same-sit restart, direct same-clip playback restart without shifting its new clock, and contact-reference replacement invalidation. All pass. The same-sit restart defect originally reproduced once per rig; its five-failure result is retained as `review-body-capsules-restart-rejected.json` and matching log. The corrected carrier records playback starts and reference epoch. Public same-sit restart rejects while body ownership is active; changing the playback or reference invalidates the old capture. Clearing only compensates unchanged captured playback clocks.

The wrapper now returns an empty result for unsupported/missing body geometry before adding articulation metadata. A separate isolated wrapper fixture, `review_body_empty.gd`, injects empty geometry behind an otherwise-ready carrier and confirms rejection. It passes; this final one-line guard does not affect supported-rig poses or sampling. Result logs are `review-body-capsules.log` and `review-body-empty.log`. No runtime files or Windows processes were modified by the reviewer.
