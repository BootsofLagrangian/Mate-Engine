# Independent recovered-source candidate review

**APPROVED as source recovery and a diagnostic experiment. No candidate is approved for production enablement.** Runtime defaults remain unchanged.

Independently re-extracted the original local CySpring bundle using the supplied extractor and UnityPy in Python 3.12. The resulting parsed JSON exactly matches the saved profile: four cape chains, 18 joint records, 27 collider records and four connected-panel records. Source hash and profile identity are retained in `../cape_audit/independent-cyspring-extraction.json`. This establishes reproducible extraction and correspondence, not compatible solver units or implementation of the source limits/colliders/panel coupling.

Source inspection confirms that each selected runtime spring resource is shallow-duplicated before replacing numeric arrays. Existing collider identities remain shared as intended; unrelated chains, including the original shared tail group, are not retuned. Both diagnostic avatars use actual enabled SkeletonModifiers at the same origin, with explicit initial rebase, and are visible before measured source steps. Source poses are held during sequential image readback. This is a bounded sampled comparison, not a temporally identical replay of the full desktop application.

Review found and resolved a candidate-configuration error: initial array anchors were additionally multiplied by the imported group stiffness/gravity scales. The original run and source are preserved as scale-confounded history. The final rerun explicitly sets those two scales to one on the isolated candidate resources and records scales and arrays at every sampled point. **5,040 independent checks passed** over all 84 recorded samples: effective stiffness/gravity arrays match the extracted source ratios; selected-chain drag/radius/collider identities remain unchanged; every recorded nonselected-chain configuration exactly matches its original capture. `independent-validation.json` retains the checks and independently recomputed geometric proxies. The corrected run records 4,488 actual modifier callbacks.

Inspected the final centered oblique baseline/candidate renders for relaxed rest, overhead stretch and arm stretch. Effective anchor 1.2 makes the cape visibly narrower and more downward hanging in these samples. At source time one second in overhead stretch, recorded cape-bone X extent falls from 1.127857 m to 0.820705 m, while average root-to-last-recorded-tip vertical drop increases from 0.495197 m to 0.591822 m. These are **bone extent/drop proxies**, not rendered-mesh width or strain. Anchor 5.0 visibly collapses the panels into narrow strips near the body in the inspected overhead sample; stronger is not automatically better.

All four corrected anchors (0.6, 1.2, 5.0, 12.5) are retained. The wider sweep was chosen after observing the first scale-confounded attempts; this is an adaptive exploration, not a preselected validation set. The inspected improvement at 1.2 supports further investigation only. Missing source angular limits, panel constraints, collider semantics and solver normalization remain material. Dense temporal behavior, skinned-mesh strain/intersection, sitting/turning interactions and the full Windows camera were not validated here. No blanket garment-naturalness, collision-freedom or authentic-game-physics claim is approved.

The reviewed source and retained diagnostic identities are in `identities.json`; this reviewer changed no runtime settings or production physics code.
