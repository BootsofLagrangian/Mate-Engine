# Staging and legacy approach follow-up

APPROVED for the bounded source scope. The legacy furniture approach exemption requires an actual approaching interaction and its exact owned Director request ID. It rejects unrelated active work or a queue containing unrelated requests, while restoring dispatch for the internal move that was previously blocked by its own furniture-busy state.

The scene heading speed factor is a smoothstep of nonnegative cosine: rear-hemisphere forward travel reaches zero continuously, with zero slope at acquisition. Explicit user stop can retain canonical ground ownership after resolving the route; urgent drag/disable/character/shutdown releases remain intact.

The imported-mesh collision representation now separates triangle-connected components (including welded coincident vertices), rather than treating disconnected material batches as solid empty space. Each component still uses a conservative transformed AABB; the target object remains included. Staging tries only source XZ distance factors 1.0 through 1.25 and runs the same full-solid path admission for every candidate. Failure does not omit geometry to force a pass.

This review applies to static imported triangle furniture geometry and standing navigation clearance. It does not establish full animated mesh/garment collision or natural choreography. Actual Windows legacy computer completion and scene approach/turn rendering remain required acceptance evidence. No runtime changes were made by this reviewer.
